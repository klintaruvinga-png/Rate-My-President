# Inlined at build time by Vite for production deployments (Vercel) so the
# deployed frontend always targets the live Railway backend. Local dev should
# use .env (or omit this file) to point at http://localhost:3001.
VITE_API_BASE_URL=https://rate-my-president-production.up.railway.app
